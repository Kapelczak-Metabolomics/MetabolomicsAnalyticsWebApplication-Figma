services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: metabo
      POSTGRES_PASSWORD: metabo_secret
      POSTGRES_DB: metaboanalytics
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "${METABO_DB_PORT:-47823}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U metabo -d metaboanalytics"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  python:
    build:
      context: ./python
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      PYTHONUNBUFFERED: "1"
    ports:
      - "${METABO_PYTHON_PORT:-47824}:47824"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:47824/health')\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  api:
    build:
      context: ./server
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://metabo:metabo_secret@db:5432/metaboanalytics
      JWT_SECRET: ${JWT_SECRET:-metaboanalytics-production-secret-change-me}
      PORT: ${METABO_API_PORT:-47822}
      PYTHON_SERVICE_URL: http://python:47824
      USE_PYTHON_ANALYSIS: "true"
      RAW_DATA_DIR: /data/raw
      SEED_DEMO_DATA: ${SEED_DEMO_DATA:-true}
    ports:
      - "${METABO_API_PORT:-47822}:${METABO_API_PORT:-47822}"
    volumes:
      - raw_data:/data/raw
    depends_on:
      db:
        condition: service_healthy
      python:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"const p=process.env.PORT||47822;require('http').get('http://127.0.0.1:'+p+'/api/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        METABO_API_PORT: ${METABO_API_PORT:-47822}
    restart: unless-stopped
    ports:
      - "${METABO_WEB_PORT:-47821}:80"
    depends_on:
      api:
        condition: service_healthy

volumes:
  postgres_data:
  raw_data:
